# ========================================
# VPS-3 — Authentication Stack Environment
# ========================================
# Copy to .env.vps3 and fill in real values:
#   cp .env.vps3.template .env.vps3
#   nano .env.vps3
#
# Usage:
#   docker compose -f docker-compose.auth-vps.yml --env-file .env.vps3 up -d

# LDAP Admin Password (cn=admin,dc=isic,dc=ma)
# Used by: OpenLDAP, CAS, phpLDAPadmin, init-ldap-vps.sh
LDAP_ADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# LDAP Config Password (cn=admin,cn=config)
# Used by: OpenLDAP internal config
LDAP_CONFIG_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# CAS Admin Password (optional, for CAS management endpoints)
CAS_ADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
