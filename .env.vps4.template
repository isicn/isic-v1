# VPS-4 — Odoo Production + Staging
# Copy: cp .env.vps4.template .env.vps4
# Usage: docker compose -f docker/docker-compose.vps4.yml --env-file .env.vps4 up -d

# --- Production ---
PROD_DB_USER=odoo_prod
PROD_DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
PROD_DB_NAME=isic_prod
PROD_ADMIN_PASSWD=CHANGE_ME_MASTER_PASSWORD
PROD_WORKERS=4

# --- Staging ---
STAGING_DB_USER=odoo_staging
STAGING_DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
STAGING_DB_NAME=isic_staging
STAGING_ADMIN_PASSWD=CHANGE_ME_MASTER_PASSWORD
STAGING_WORKERS=2

# --- SMTP ---
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=notifications@isic.ac.ma
SMTP_PASSWORD=CHANGE_ME_APP_PASSWORD
SMTP_SSL=True

# --- Backup ---
BACKUP_RETENTION_DAYS=7
